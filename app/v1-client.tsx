"use client";

import { FormEvent, useEffect, useState } from "react";
import { CalendarDays, Check, ChevronRight, ClipboardList, FileText, FileUp, Home, LogOut, MoreHorizontal, Package, Plus, ReceiptText, Settings, ShieldCheck, UsersRound, Wrench } from "lucide-react";

type View = "today" | "quotes" | "orders" | "planning" | "more" | "customers" | "catalog" | "reports" | "billing" | "admin";
type Row = Record<string, any>;

const sideModules = [
  ["today", "Heute", Home], ["quotes", "Angebote", FileText], ["orders", "Aufträge", Wrench], ["planning", "Kalender", CalendarDays],
  ["reports", "Rapporte", ClipboardList], ["billing", "Abrechnung", ReceiptText], ["customers", "Kunden", UsersRound], ["catalog", "Artikel & Leistungen", Package], ["admin", "Verwaltung", Settings],
] as const;
const bottomModules = [["today", "Heute", Home], ["quotes", "Angebote", FileText], ["orders", "Aufträge", Wrench], ["planning", "Kalender", CalendarDays], ["more", "Mehr", MoreHorizontal]] as const;

async function api(path: string, init?: RequestInit) {
  const res = await fetch(path, { ...init, headers: { "Content-Type": "application/json", ...(init?.headers || {}) } });
  const body = await res.json().catch(() => ({}));
  if (!res.ok) throw new Error(body.error || `HTTP ${res.status}`);
  return body;
}

export default function V1Client() {
  const [loading, setLoading] = useState(true), [installed, setInstalled] = useState(false), [user, setUser] = useState<Row | null>(null), [view, setView] = useState<View>("today");
  async function boot() { setLoading(true); try { const status = await api("/api/auth/status"); setInstalled(status.installed); setUser(status.user); } finally { setLoading(false); } }
  useEffect(() => { boot(); }, []);
  if (loading) return <Splash text="Kontrollraum wird vorbereitet …" />;
  if (!installed) return <FirstSetup onDone={boot} />;
  if (!user) return <Login onDone={boot} />;
  async function logout() { await api("/api/auth/logout", { method: "POST" }); setUser(null); }
  return <Shell company={user.company} user={user} view={view} setView={setView} onLogout={logout}>
    {view === "today" && <Today company={user.company} setView={setView} />}
    {view === "quotes" && <Quotes setView={setView} />}
    {view === "orders" && <Orders />}
    {view === "planning" && <Planning />}
    {view === "more" && <More setView={setView} />}
    {view === "reports" && <Reports setView={setView} />}
    {view === "billing" && <Billing />}
    {view === "customers" && <Customers />}
    {view === "catalog" && <Catalog />}
    {view === "admin" && <Admin onCompanyChanged={boot} />}
  </Shell>;
}

function FirstSetup({ onDone }: { onDone: () => void }) {
  const [step, setStep] = useState<1 | 2>(1), [admin, setAdmin] = useState({ fullName: "", email: "", password: "" }), [error, setError] = useState("");
  function adminSubmit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const f = new FormData(e.currentTarget); const next = { fullName: String(f.get("fullName")), email: String(f.get("email")), password: String(f.get("password")) }; if (next.password.length < 8) return setError("Das Passwort muss mindestens 8 Zeichen lang sein."); setAdmin(next); setError(""); setStep(2); }
  async function companySubmit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); setError(""); const f = new FormData(e.currentTarget); try { await api("/api/auth/setup", { method: "POST", body: JSON.stringify({ ...admin, companyName: f.get("companyName"), street: f.get("street"), zip: f.get("zip"), city: f.get("city"), companyEmail: f.get("companyEmail"), phone: f.get("phone") }) }); await onDone(); } catch (e: any) { setError(e.message); } }
  if (step === 1) return <main className="install-shell"><form className="install-card" onSubmit={adminSubmit}><Logo/><p className="eyebrow">Erste Installation · 1 von 2</p><h1>Administratorkonto anlegen</h1><p>Das erste Konto erhält Vollzugriff. Weitere Rollen und Benutzer können später angelegt werden.</p><Field name="fullName" label="Dein Name" required/><Field name="email" label="E-Mail-Adresse" type="email" required/><Field name="password" label="Passwort" type="password" required/>{error&&<div className="form-error">{error}</div>}<button className="primary-btn full">Weiter<ChevronRight size={18}/></button></form></main>;
  return <main className="install-shell"><form className="install-card wide" onSubmit={companySubmit}><Logo/><p className="eyebrow">Erste Installation · 2 von 2</p><h1>Unternehmen einrichten</h1><p>Kontrollraum startet danach vollständig leer.</p><div className="form-grid"><Field name="companyName" label="Unternehmensname" required/><Field name="companyEmail" label="Geschäftliche E-Mail" type="email" defaultValue={admin.email}/><Field name="street" label="Straße / Hausnummer"/><Field name="zip" label="PLZ"/><Field name="city" label="Ort"/><Field name="phone" label="Telefon"/></div>{error&&<div className="form-error">{error}</div>}<button className="primary-btn full">Kontrollraum einrichten<Check size={18}/></button><button type="button" className="text-btn" onClick={()=>setStep(1)}>Zurück</button></form></main>;
}

function Login({ onDone }: { onDone: () => void }) { const [error,setError]=useState(""); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");const f=new FormData(e.currentTarget);try{await api("/api/auth/login",{method:"POST",body:JSON.stringify({email:f.get("email"),password:f.get("password")})});await onDone()}catch(e:any){setError(e.message)}} return <main className="install-shell"><form className="install-card" onSubmit={submit}><Logo/><p className="eyebrow">Anmeldung</p><h1>Willkommen zurück</h1><p>Melde dich an deiner lokalen Kontrollraum-Installation an.</p><Field name="email" label="E-Mail-Adresse" type="email" required/><Field name="password" label="Passwort" type="password" required/>{error&&<div className="form-error">{error}</div>}<button className="primary-btn full">Anmelden<ChevronRight size={18}/></button></form></main>; }
function Splash({text}:{text:string}) { return <main className="install-shell"><div className="install-card"><Logo/><div className="pulse"/><p>{text}</p></div></main>; }
function Logo({company}:{company?:string}) { return <div className="brand"><div className="brand-mark"><span/><span/><span/></div><div><strong>Kontrollraum</strong><small>{company || "Unternehmenssoftware"}</small></div></div>; }

function Shell({company,user,view,setView,onLogout,children}:{company:Row;user:Row;view:View;setView:(v:View)=>void;onLogout:()=>void;children:any}) {
 return <div className="app-shell"><aside className="sidebar"><Logo company={company?.name}/><nav className="side-nav">{sideModules.map(([id,label,Icon])=><button key={id} className={`nav-item ${view===id?"active":""}`} onClick={()=>setView(id)}><Icon size={19}/>{label}</button>)}</nav><div className="side-bottom"><div className="local-badge"><ShieldCheck size={16}/><span><b>Local-First</b><small>PostgreSQL lokal</small></span></div><div className="profile-card"><div className="avatar">{initials(user.fullName)}</div><div><b>{user.fullName}</b><small>Administrator</small></div><button className="mini-icon" onClick={onLogout}><LogOut size={15}/></button></div></div></aside><main className="main"><header className="mobile-header"><Logo company={company?.name}/><button className="icon-btn" onClick={onLogout}><LogOut size={19}/></button></header>{children}</main><nav className="bottom-nav">{bottomModules.map(([id,label,Icon])=><button key={id} className={view===id?"active":""} onClick={()=>setView(id)}><Icon size={20}/><span>{label}</span></button>)}</nav></div>;
}

function Today({company,setView}:{company:Row;setView:(v:View)=>void}) {
 const [summary,setSummary]=useState({quotes:0,review:0,warnings:0,invoices:0});
 useEffect(()=>{Promise.all([api("/api/quotes").catch(()=>[]),api("/api/orders").catch(()=>[]),api("/api/planning").catch(()=>({warnings:[]})),api("/api/billing").catch(()=>[])]).then(([q,o,p,i])=>setSummary({quotes:q.filter((x:Row)=>["DRAFT","SENT"].includes(x.status)).length,review:o.filter((x:Row)=>x.status==="READY_FOR_REVIEW").length,warnings:p.warnings?.length||0,invoices:i.filter((x:Row)=>["SENT","PARTIALLY_PAID","OVERDUE"].includes(x.status)).length}))},[]);
 return <Page title={`Guten Tag${company?.name?", "+company.name:""}`} eyebrow="Heute" subtitle="Was im Betrieb als Nächstes Aufmerksamkeit braucht."><div className="fact-grid"><button onClick={()=>setView("quotes")}><span>Offene Angebote</span><b>{summary.quotes}</b></button><button onClick={()=>setView("planning")}><span>Planungswarnungen</span><b>{summary.warnings}</b></button><button onClick={()=>setView("orders")}><span>Zur Prüfung</span><b>{summary.review}</b></button><button onClick={()=>setView("billing")}><span>Offene Rechnungen</span><b>{summary.invoices}</b></button></div><section className="empty-hero"><div className="priority-icon"><FileText/></div><div><span>Standardprozess</span><h2>Angebot → Auftrag → Abrechnung</h2><p>Für kleine Arbeiten steht zusätzlich Rapport → Abrechnung zur Verfügung.</p></div><button className="primary-btn" onClick={()=>setView("quotes")}>Angebot anlegen</button></section></Page>;
}

function Quotes({setView}:{setView:(v:View)=>void}) {
 const [rows,setRows]=useState<Row[]>([]),[customers,setCustomers]=useState<Row[]>([]),[catalog,setCatalog]=useState<Row[]>([]),[open,setOpen]=useState(false),[detail,setDetail]=useState<Row|null>(null),[error,setError]=useState("");
 async function load(){const [q,c,k]=await Promise.all([api("/api/quotes"),api("/api/customers"),api("/api/catalog")]);setRows(q);setCustomers(c);setCatalog(k)} useEffect(()=>{load()},[]);
 async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");const f=new FormData(e.currentTarget);const item=catalog.find(x=>x.id===String(f.get("catalogItemId")||""));try{await api("/api/quotes",{method:"POST",body:JSON.stringify({customerId:f.get("customerId"),title:f.get("title"),description:f.get("description"),validUntil:f.get("validUntil"),items:item?[{catalogItemId:item.id,description:item.name,quantity:num(f.get("quantity"))||1,unit:item.unit,unitPrice:Number(item.salesPrice),taxRate:Number(item.taxRate)}]:[]})});setOpen(false);load()}catch(e:any){setError(e.message)}}
 async function action(name:string,extra:Row={}){if(!detail)return;try{await api(`/api/quotes/${detail.id}`,{method:"PATCH",body:JSON.stringify({action:name,...extra})});setDetail(null);await load();if(name==="convert")setView("orders")}catch(e:any){setError(e.message)}}
 return <Page title="Angebote" eyebrow="Vertrieb" subtitle="Erstellen, versenden, Rückmeldung erfassen und angenommene Angebote in Aufträge überführen." action={<button className="primary-btn" onClick={()=>setOpen(true)} disabled={!customers.length}><Plus size={17}/>Angebot</button>}><div className="list-card">{rows.length?rows.map(x=><button className="customer-row" key={x.id} onClick={()=>setDetail(x)}><div className="customer-avatar"><FileText size={17}/></div><div><b>{x.number} · {x.customer?.name}</b><span>{x.title} · {money(x.grossTotal)}</span></div><small>{quoteStatus(x.status)}</small><ChevronRight size={17}/></button>):<Empty text={customers.length?"Noch keine Angebote.":"Lege zuerst einen Kunden an."}/>}</div>{open&&<Modal title="Angebot anlegen" close={()=>setOpen(false)}><form onSubmit={save}><Select name="customerId" label="Kunde" options={customers.map(x=>({value:x.id,label:x.name}))}/><Field name="title" label="Titel / Projekt" required/><Field name="description" label="Beschreibung"/><Field name="validUntil" label="Gültig bis" type="date"/><Select name="catalogItemId" label="Erste Position (optional)" options={[{value:"",label:"Keine"},...catalog.map(x=>({value:x.id,label:x.name}))]}/><Field name="quantity" label="Menge" type="number" step="0.001" defaultValue="1"/>{error&&<div className="form-error">{error}</div>}<button className="primary-btn full">Angebot anlegen</button></form></Modal>}{detail&&<Modal title={`${detail.number} · ${detail.title}`} close={()=>setDetail(null)}><p><b>{detail.customer?.name}</b> · {quoteStatus(detail.status)}</p><div className="fact-grid"><div><span>Netto</span><b>{money(detail.netTotal)}</b></div><div><span>Brutto</span><b>{money(detail.grossTotal)}</b></div><div><span>Version</span><b>{detail.version}</b></div><div><span>Positionen</span><b>{detail.items?.length||0}</b></div></div>{error&&<div className="form-error">{error}</div>}<div className="button-row">{detail.status==="DRAFT"&&<button className="primary-btn" onClick={()=>action("send")}>Als versendet markieren</button>}{detail.status==="SENT"&&<><button className="primary-btn" onClick={()=>action("accept")}>Angenommen</button><button className="secondary-btn" onClick={()=>action("reject")}>Abgelehnt</button></>}{detail.status==="REJECTED"&&<button className="secondary-btn" onClick={()=>action("archive")}>Archivieren</button>}{detail.status==="ACCEPTED"&&<form onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);action("convert",{plannedMinutes:Math.round(num(f.get("plannedHours"))*60)})}}><Field name="plannedHours" label="Geplanter Zeitansatz (Stunden)" type="number" step="0.25"/><button className="primary-btn full">In Auftrag umwandeln</button></form>}</div></Modal>}</Page>;
}

function Orders() {
 const [rows,setRows]=useState<Row[]>([]),[customers,setCustomers]=useState<Row[]>([]),[catalog,setCatalog]=useState<Row[]>([]),[open,setOpen]=useState(false),[detail,setDetail]=useState<Row|null>(null);
 async function load(){const [o,c,k]=await Promise.all([api("/api/orders"),api("/api/customers"),api("/api/catalog")]);setRows(o);setCustomers(c);setCatalog(k)} useEffect(()=>{load()},[]);
 async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const item=catalog.find(x=>x.id===String(f.get("catalogItemId")||""));await api("/api/orders",{method:"POST",body:JSON.stringify({customerId:f.get("customerId"),title:f.get("title"),description:f.get("description"),plannedMinutes:Math.round(num(f.get("plannedHours"))*60),items:item?[{catalogItemId:item.id,description:item.name,quantity:num(f.get("quantity"))||1,unit:item.unit,unitPrice:Number(item.salesPrice),taxRate:Number(item.taxRate)}]:[]})});setOpen(false);load()}
 async function addDoc(action:string,e:FormEvent<HTMLFormElement>){e.preventDefault();if(!detail)return;const f=new FormData(e.currentTarget);const body:any={action};for(const [k,v] of f.entries())body[k]=["quantity","unitPrice","amount","minutes","hourlyRate"].includes(k)?num(v):v;await api(`/api/orders/${detail.id}`,{method:"PATCH",body:JSON.stringify(body)});setDetail(await api(`/api/orders/${detail.id}`));load()}
 return <Page title="Aufträge" eyebrow="Arbeit" subtitle="Planung, Vorbereitung, Ausführung, Dokumentation und Chefprüfung." action={<button className="primary-btn" onClick={()=>setOpen(true)} disabled={!customers.length}><Plus size={17}/>Direktauftrag</button>}><div className="list-card">{rows.length?rows.map(x=><button className="customer-row" key={x.id} onClick={async()=>setDetail(await api(`/api/orders/${x.id}`))}><div className="customer-avatar"><Wrench size={17}/></div><div><b>{x.number} · {x.customer?.name}</b><span>{x.title}{x.plannedMinutes?` · ${hours(x.plannedMinutes)} h geplant`:""}</span></div><small>{orderStatus(x.status)}</small><ChevronRight size={17}/></button>):<Empty text="Noch keine Aufträge. Normalerweise entsteht ein Auftrag aus einem angenommenen Angebot."/>}</div>{open&&<Modal title="Direktauftrag anlegen" close={()=>setOpen(false)}><p>Für Notdienst, Stammkundenauftrag oder Arbeiten ohne vorheriges Angebot.</p><form onSubmit={save}><Select name="customerId" label="Kunde" options={customers.map(x=>({value:x.id,label:x.name}))}/><Field name="title" label="Auftrag / Projekt" required/><Field name="description" label="Beschreibung"/><Field name="plannedHours" label="Zeitansatz in Stunden" type="number" step="0.25"/><Select name="catalogItemId" label="Erste Position (optional)" options={[{value:"",label:"Keine"},...catalog.map(x=>({value:x.id,label:x.name}))]}/><Field name="quantity" label="Menge" type="number" step="0.001" defaultValue="1"/><button className="primary-btn full">Direktauftrag anlegen</button></form></Modal>}{detail&&<Modal title={`${detail.number} · ${detail.title}`} close={()=>setDetail(null)}><p><b>{detail.customer?.name}</b> · {orderStatus(detail.status)}</p><div className="fact-grid"><div><span>Soll-Positionen</span><b>{detail.items?.length||0}</b></div><div><span>Material Ist</span><b>{detail.materials?.length||0}</b></div><div><span>Nachträge</span><b>{detail.extras?.length||0}</b></div><div><span>Zeiten</span><b>{detail.timeEntries?.length||0}</b></div></div><h3>Materialverbrauch</h3><form onSubmit={e=>addDoc("material",e)}><div className="form-grid"><Field name="description" label="Material" required/><Field name="quantity" label="Menge" type="number" step="0.001" defaultValue="1"/><Field name="unit" label="Einheit" defaultValue="Stk"/><Field name="unitPrice" label="Abrechnungspreis" type="number" step="0.01"/></div><button className="secondary-btn">Material hinzufügen</button></form><h3>Mehrbedarf / Nachtrag</h3><form onSubmit={e=>addDoc("extra",e)}><div className="form-grid"><Field name="description" label="Zusatzleistung" required/><Field name="amount" label="Netto" type="number" step="0.01"/></div><button className="secondary-btn">Nachtrag hinzufügen</button></form><button className="primary-btn full" onClick={async()=>{await api(`/api/orders/${detail.id}`,{method:"PATCH",body:JSON.stringify({status:"READY_FOR_REVIEW"})});setDetail(null);load()}}>Zur Prüfung an den Chef</button></Modal>}</Page>;
}

function Planning() {
 const [data,setData]=useState<Row|null>(null),[assignOpen,setAssignOpen]=useState(false),[absenceOpen,setAbsenceOpen]=useState(false),[error,setError]=useState("");
 async function load(){setData(await api("/api/planning"))} useEffect(()=>{load()},[]);
 async function submit(action:string,e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");const f=new FormData(e.currentTarget);try{await api("/api/planning",{method:"POST",body:JSON.stringify({action,...Object.fromEntries(f.entries())})});setAssignOpen(false);setAbsenceOpen(false);load()}catch(e:any){setError(e.message)}}
 if(!data)return <Splash text="Kalender wird geladen …"/>;
 return <Page title="Mitarbeiterkalender" eyebrow="Einsatzplanung" subtitle="Aufträge zuweisen, Zeitansatz planen und Urlaub oder Krankheit berücksichtigen." action={<div className="button-row"><button className="secondary-btn" onClick={()=>setAbsenceOpen(true)}>Abwesenheit</button><button className="primary-btn" onClick={()=>setAssignOpen(true)}>Einsatz planen</button></div>}>{data.warnings?.length>0&&<section className="panel"><h2>Planungswarnungen</h2>{data.warnings.map((w:Row,i:number)=><div className="form-error" key={i}>{w.message}</div>)}</section>}<section className="panel"><h2>Geplante Aufträge</h2><div className="list-card">{data.orders.filter((x:Row)=>x.scheduledAt).length?data.orders.filter((x:Row)=>x.scheduledAt).map((x:Row)=><div className="customer-row" key={x.id}><div className="customer-avatar"><CalendarDays size={17}/></div><div><b>{x.number} · {x.customer?.name}</b><span>{dateTime(x.scheduledAt)}{x.scheduledEndAt?` – ${dateTime(x.scheduledEndAt)}`:""}</span></div><small>{x.assignments.map((a:Row)=>a.user.fullName).join(", ")||"unbesetzt"}</small></div>):<Empty text="Noch keine Einsätze geplant."/>}</div></section><section className="panel"><h2>Abwesenheiten</h2>{data.absences.length?data.absences.map((a:Row)=><div className="diff-row" key={a.id}><div><b>{a.user.fullName}</b><span>{dateTime(a.startsAt)} – {dateTime(a.endsAt)}</span></div><strong>{absenceType(a.type)}</strong></div>):<Empty text="Keine Abwesenheiten eingetragen."/>}</section>{assignOpen&&<Modal title="Einsatz planen" close={()=>setAssignOpen(false)}><form onSubmit={e=>submit("assign",e)}><Select name="orderId" label="Auftrag" options={data.orders.map((x:Row)=>({value:x.id,label:`${x.number} · ${x.title}`}))}/><Select name="userId" label="Mitarbeiter" options={data.users.map((x:Row)=>({value:x.id,label:x.fullName}))}/><Field name="startsAt" label="Beginn" type="datetime-local" required/><Field name="endsAt" label="Ende" type="datetime-local" required/>{error&&<div className="form-error">{error}</div>}<button className="primary-btn full">Einplanen</button></form></Modal>}{absenceOpen&&<Modal title="Mitarbeiter sperren" close={()=>setAbsenceOpen(false)}><form onSubmit={e=>submit("absence",e)}><Select name="userId" label="Mitarbeiter" options={data.users.map((x:Row)=>({value:x.id,label:x.fullName}))}/><Select name="type" label="Grund" options={[{value:"VACATION",label:"Urlaub"},{value:"SICK",label:"Krank"},{value:"TRAINING",label:"Fortbildung"},{value:"TIME_OFF",label:"Zeitausgleich"},{value:"OTHER",label:"Sonstiges"}]}/><Field name="startsAt" label="Von" type="datetime-local" required/><Field name="endsAt" label="Bis" type="datetime-local" required/><Field name="note" label="Notiz"/>{error&&<div className="form-error">{error}</div>}<button className="primary-btn full">Zeitraum sperren</button></form></Modal>}</Page>;
}

function Reports({setView}:{setView:(v:View)=>void}) {
 const [rows,setRows]=useState<Row[]>([]),[customers,setCustomers]=useState<Row[]>([]),[catalog,setCatalog]=useState<Row[]>([]),[open,setOpen]=useState(false);
 async function load(){const [r,c,k]=await Promise.all([api("/api/reports"),api("/api/customers"),api("/api/catalog")]);setRows(r);setCustomers(c);setCatalog(k)} useEffect(()=>{load()},[]);
 async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const item=catalog.find(x=>x.id===String(f.get("catalogItemId")||""));await api("/api/reports",{method:"POST",body:JSON.stringify({customerId:f.get("customerId"),title:f.get("title"),description:f.get("description"),performedAt:f.get("performedAt"),workMinutes:Math.round(num(f.get("workHours"))*60),hourlyRate:num(f.get("hourlyRate")),items:item?[{catalogItemId:item.id,description:item.name,quantity:num(f.get("quantity"))||1,unit:item.unit,unitPrice:Number(item.salesPrice),taxRate:Number(item.taxRate)}]:[]})});setOpen(false);load()}
 return <Page title="Rapporte" eyebrow="Direkte Leistung" subtitle="Kleine oder spontane Arbeiten direkt dokumentieren und ohne Angebots-/Auftragsweg abrechnen." action={<button className="primary-btn" onClick={()=>setOpen(true)} disabled={!customers.length}><Plus size={17}/>Rapport</button>}><div className="list-card">{rows.length?rows.map(x=><div className="customer-row" key={x.id}><div className="customer-avatar"><ClipboardList size={17}/></div><div><b>{x.number} · {x.customer?.name}</b><span>{x.title} · {hours(x.workMinutes)} h</span></div><small>{reportStatus(x.status)}</small></div>):<Empty text="Noch keine Rapporte."/>}</div><button className="secondary-btn spaced" onClick={()=>setView("billing")}>Zur Abrechnung</button>{open&&<Modal title="Rapport anlegen" close={()=>setOpen(false)}><form onSubmit={save}><Select name="customerId" label="Kunde" options={customers.map(x=>({value:x.id,label:x.name}))}/><Field name="title" label="Ausgeführte Arbeit" required/><Field name="description" label="Dokumentation / Beschreibung"/><Field name="performedAt" label="Ausgeführt am" type="datetime-local"/><Field name="workHours" label="Arbeitszeit (Stunden)" type="number" step="0.25"/><Field name="hourlyRate" label="Stundensatz netto" type="number" step="0.01"/><Select name="catalogItemId" label="Material / Leistung (optional)" options={[{value:"",label:"Keine"},...catalog.map(x=>({value:x.id,label:x.name}))]}/><Field name="quantity" label="Menge" type="number" step="0.001" defaultValue="1"/><button className="primary-btn full">Rapport zur Prüfung anlegen</button></form></Modal>}</Page>;
}

function Billing() {
 const [invoices,setInvoices]=useState<Row[]>([]),[orders,setOrders]=useState<Row[]>([]),[reports,setReports]=useState<Row[]>([]),[error,setError]=useState("");
 async function load(){const [i,o,r]=await Promise.all([api("/api/billing"),api("/api/orders"),api("/api/reports")]);setInvoices(i);setOrders(o);setReports(r)} useEffect(()=>{load()},[]);
 const billableOrders=orders.filter(x=>["READY_FOR_REVIEW","COMPLETED"].includes(x.status)); const billableReports=reports.filter(x=>["READY_FOR_REVIEW","APPROVED"].includes(x.status)&&!x.invoices?.some((i:Row)=>i.status!=="CANCELLED"));
 async function create(body:Row){setError("");try{await api("/api/billing",{method:"POST",body:JSON.stringify(body)});load()}catch(e:any){setError(e.message)}}
 return <Page title="Abrechnung" eyebrow="Rechnungen" subtitle="Aufträge und Rapporte werden hier geprüft und zu Rechnungen.">{error&&<div className="form-error">{error}</div>}<h2>Aufträge zur Abrechnung</h2><div className="list-card">{billableOrders.length?billableOrders.map(x=><div className="customer-row" key={x.id}><div className="customer-avatar"><ReceiptText size={17}/></div><div><b>{x.customer?.name}</b><span>{x.number} · {x.title}</span></div><button className="secondary-btn" onClick={()=>create({orderId:x.id})}>Rechnungsvorschlag</button></div>):<Empty text="Keine Aufträge warten auf Abrechnung."/>}</div><h2 className="spaced">Rapporte zur Abrechnung</h2><div className="list-card">{billableReports.length?billableReports.map(x=><div className="customer-row" key={x.id}><div className="customer-avatar"><ClipboardList size={17}/></div><div><b>{x.customer?.name}</b><span>{x.number} · {x.title}</span></div><button className="secondary-btn" onClick={()=>create({reportId:x.id})}>Rechnungsvorschlag</button></div>):<Empty text="Keine Rapporte warten auf Abrechnung."/>}</div><h2 className="spaced">Rechnungen</h2><div className="list-card">{invoices.length?invoices.map(x=><div className="customer-row" key={x.id}><div className="customer-avatar"><ReceiptText size={17}/></div><div><b>{x.number} · {x.customer?.name}</b><span>{x.order?.title||x.report?.title||"Rechnung"} · {invoiceStatus(x.status)}</span></div><strong>{money(x.grossTotal)}</strong></div>):<Empty text="Noch keine Rechnungen."/>}</div></Page>;
}

function More({setView}:{setView:(v:View)=>void}) { const links:[[View,string,any,string],...Array<[View,string,any,string]>]=[["reports","Rapporte",ClipboardList,"Direkte Leistung → Abrechnung"],["billing","Abrechnung",ReceiptText,"Rechnungen und offene Posten"],["customers","Kunden",UsersRound,"Stammdaten"],["catalog","Artikel & Leistungen",Package,"Material und Leistungen"],["admin","Verwaltung",Settings,"Mitarbeiter, Rollen und Betrieb"]]; return <Page title="Mehr" eyebrow="Module" subtitle="Weitere Bereiche von Kontrollraum."><div className="list-card">{links.map(([id,label,Icon,sub])=><button className="customer-row" key={id} onClick={()=>setView(id)}><div className="customer-avatar"><Icon size={17}/></div><div><b>{label}</b><span>{sub}</span></div><ChevronRight size={17}/></button>)}</div></Page>; }

function Customers() { const [rows,setRows]=useState<Row[]>([]),[open,setOpen]=useState(false),[q,setQ]=useState(""); async function load(){setRows(await api("/api/customers"))} useEffect(()=>{load()},[]); async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await api("/api/customers",{method:"POST",body:JSON.stringify({name:f.get("name"),contactPerson:f.get("contactPerson"),street:f.get("street"),zip:f.get("zip"),city:f.get("city"),email:f.get("email"),phone:f.get("phone")})});setOpen(false);load()} const filtered=rows.filter(x=>x.name.toLowerCase().includes(q.toLowerCase())); return <Page title="Kunden" eyebrow="Stammdaten" subtitle="Einmal erfassen, im gesamten Prozess verwenden." action={<button className="primary-btn" onClick={()=>setOpen(true)}><Plus size={17}/>Kunde</button>}><Search value={q} setValue={setQ}/><div className="list-card">{filtered.length?filtered.map(x=><div className="customer-row" key={x.id}><div className="customer-avatar">{initials(x.name)}</div><div><b>{x.name}</b><span>{[x.street,x.zip,x.city].filter(Boolean).join(" · ")||"Keine Anschrift"}</span></div><small>{x.number||"–"}</small></div>):<Empty text="Noch keine Kunden angelegt."/>}</div>{open&&<Modal title="Kunde anlegen" close={()=>setOpen(false)}><form onSubmit={save}><div className="form-grid"><Field name="name" label="Name / Firma" required/><Field name="contactPerson" label="Ansprechpartner"/><Field name="street" label="Straße"/><Field name="zip" label="PLZ"/><Field name="city" label="Ort"/><Field name="email" label="E-Mail" type="email"/><Field name="phone" label="Telefon"/></div><button className="primary-btn full">Speichern</button></form></Modal>}</Page>; }

function Catalog() { const [rows,setRows]=useState<Row[]>([]),[open,setOpen]=useState(false),[importOpen,setImportOpen]=useState(false); async function load(){setRows(await api("/api/catalog"))} useEffect(()=>{load()},[]); async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await api("/api/catalog",{method:"POST",body:JSON.stringify({name:f.get("name"),description:f.get("description"),itemType:f.get("itemType"),unit:f.get("unit"),purchasePrice:num(f.get("purchasePrice")),salesPrice:num(f.get("salesPrice")),taxRate:num(f.get("taxRate"))||19})});setOpen(false);load()} async function importCsv(file:File){const text=await file.text();const lines=text.split(/\r?\n/).filter(Boolean);if(lines.length<2)return;const h=lines[0].split(";").map(x=>x.trim().toLowerCase());const get=(p:string[],n:string)=>{const i=h.indexOf(n);return i>=0?p[i]:""};const items=lines.slice(1).map(line=>{const p=line.split(";");return{sku:get(p,"sku")||null,name:get(p,"name"),description:get(p,"description"),unit:get(p,"unit")||"Stk",purchasePrice:num(get(p,"purchase_price")),salesPrice:num(get(p,"sales_price")),taxRate:num(get(p,"tax_rate"))||19}}).filter(x=>x.name);await api("/api/catalog/import",{method:"POST",body:JSON.stringify({fileName:file.name,items})});setImportOpen(false);load()} return <Page title="Artikel & Leistungen" eyebrow="Katalog" subtitle="Material, Leistungen und eigene Preise." action={<div className="button-row"><button className="secondary-btn" onClick={()=>setImportOpen(true)}><FileUp size={16}/>Import</button><button className="primary-btn" onClick={()=>setOpen(true)}><Plus size={17}/>Neu</button></div>}><div className="table-card"><div className="table-head"><span>Artikel</span><span>Typ</span><span>EK</span><span>VK</span></div>{rows.length?rows.map(x=><div className="table-row" key={x.id}><div><b>{x.name}</b><small>{x.sku||"–"}</small></div><span>{x.itemType}</span><span>{money(x.purchasePrice)}</span><strong>{money(x.salesPrice)}</strong></div>):<Empty text="Noch keine Artikel oder Leistungen."/>}</div>{open&&<Modal title="Artikel / Leistung anlegen" close={()=>setOpen(false)}><form onSubmit={save}><div className="form-grid"><Field name="name" label="Bezeichnung" required/><Field name="description" label="Beschreibung"/><Select name="itemType" label="Typ" options={["MATERIAL","LEISTUNG"]}/><Field name="unit" label="Einheit" defaultValue="Stk"/><Field name="purchasePrice" label="Einkaufspreis" type="number" step="0.01"/><Field name="salesPrice" label="Verkaufspreis" type="number" step="0.01"/><Field name="taxRate" label="USt. %" type="number" defaultValue="19"/></div><button className="primary-btn full">Speichern</button></form></Modal>}{importOpen&&<Modal title="Großhandels-/CSV-Import" close={()=>setImportOpen(false)}><p>Aktuell unterstützt: Semikolon-CSV. DATANORM/IDS folgen als Adapter.</p><input type="file" accept=".csv,text/csv" onChange={e=>e.target.files?.[0]&&importCsv(e.target.files[0])}/></Modal>}</Page>; }

function Admin({onCompanyChanged}:{onCompanyChanged:()=>void}) { const [data,setData]=useState<Row|null>(null),[roleOpen,setRoleOpen]=useState(false); async function load(){setData(await api("/api/admin"))} useEffect(()=>{load()},[]); async function saveCompany(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await api("/api/admin",{method:"PATCH",body:JSON.stringify(Object.fromEntries(f.entries()))});await load();onCompanyChanged()} async function saveRole(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!data)return;const f=new FormData(e.currentTarget);const mods=data.modules.filter((m:Row)=>f.get(`m_${m.code}`)==="on").map((m:Row)=>m.code);await api("/api/admin",{method:"POST",body:JSON.stringify({name:f.get("name"),description:f.get("description"),modules:mods,canCreate:true,canEdit:true})});setRoleOpen(false);load()} if(!data)return <Splash text="Verwaltung wird geladen …"/>; const c=data.company; return <Page title="Verwaltung" eyebrow="Betrieb" subtitle="Unternehmensdaten, Benutzer, Rollen und Module."><section className="panel"><h2>Unternehmensdaten</h2><form onSubmit={saveCompany}><div className="form-grid"><Field name="name" label="Unternehmensname" defaultValue={c.name} required/><Field name="legalName" label="Firmierung" defaultValue={c.legalName||""}/><Field name="street" label="Straße" defaultValue={c.street||""}/><Field name="zip" label="PLZ" defaultValue={c.zip||""}/><Field name="city" label="Ort" defaultValue={c.city||""}/><Field name="email" label="E-Mail" defaultValue={c.email||""}/><Field name="phone" label="Telefon" defaultValue={c.phone||""}/><Field name="taxNumber" label="Steuernummer" defaultValue={c.taxNumber||""}/><Field name="vatId" label="USt-IdNr." defaultValue={c.vatId||""}/><Field name="iban" label="IBAN" defaultValue={c.iban||""}/></div><button className="primary-btn">Speichern</button></form></section><section className="panel"><div className="panel-head"><div><h2>Rollen & Module</h2><p>Jede Rolle sieht nur die zugeordneten Module.</p></div><button className="secondary-btn" onClick={()=>setRoleOpen(true)}><Plus size={16}/>Rolle</button></div>{data.roles.map((r:Row)=><div className="diff-row" key={r.id}><div><b>{r.name}</b><span>{r.roleModules.map((x:Row)=>x.moduleCode).join(" · ")}</span></div><strong>{r.systemRole?"System":"Eigene Rolle"}</strong></div>)}</section>{roleOpen&&<Modal title="Rolle anlegen" close={()=>setRoleOpen(false)}><form onSubmit={saveRole}><Field name="name" label="Rollenname" required/><Field name="description" label="Beschreibung"/><div className="module-checks">{data.modules.map((m:Row)=><label key={m.code}><input type="checkbox" name={`m_${m.code}`}/><span><b>{m.label}</b><small>{m.description}</small></span></label>)}</div><button className="primary-btn full">Rolle speichern</button></form></Modal>}</Page>; }

function Page({title,eyebrow,subtitle,action,children}:{title:string;eyebrow:string;subtitle:string;action?:any;children:any}){return <div className="page-wrap"><div className="page-head"><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p>{subtitle}</p></div>{action}</div>{children}</div>}
function Empty({text}:{text:string}){return <div className="empty-row"><span>{text}</span></div>}
function Search({value,setValue}:{value:string;setValue:(v:string)=>void}){return <div className="search-field"><input value={value} onChange={e=>setValue(e.target.value)} placeholder="Suchen …"/></div>}
function Modal({title,close,children}:{title:string;close:()=>void;children:any}){return <div className="drawer-backdrop modal-bg" onMouseDown={close}><div className="drawer modal" onMouseDown={e=>e.stopPropagation()}><div className="drawer-head"><b>{title}</b><button className="icon-btn" onClick={close}>×</button></div>{children}</div></div>}
function Field(p:{name:string;label:string;type?:string;required?:boolean;defaultValue?:string;step?:string}){return <label className="field"><span>{p.label}</span><input {...p}/></label>}
function Select({name,label,options}:{name:string;label:string;options:(string|{value:string;label:string})[]}){return <label className="field"><span>{label}</span><select name={name}>{options.map((o,i)=>typeof o==="string"?<option key={o} value={o}>{o}</option>:<option key={o.value+String(i)} value={o.value}>{o.label}</option>)}</select></label>}
function initials(v:string=""){return v.split(/\s+/).filter(Boolean).slice(0,2).map(x=>x[0]).join("").toUpperCase()||"KR"}
function num(v:any){const n=Number(String(v??"").replace(",","."));return Number.isFinite(n)?n:0}
function money(v:any){if(v===null||v===undefined||v==="")return "–";return new Intl.NumberFormat("de-DE",{style:"currency",currency:"EUR"}).format(Number(v))}
function hours(v:any){return (Number(v||0)/60).toLocaleString("de-DE",{maximumFractionDigits:2})}
function dateTime(v:any){if(!v)return "–";return new Intl.DateTimeFormat("de-DE",{dateStyle:"short",timeStyle:"short"}).format(new Date(v))}
function quoteStatus(v:string){return ({DRAFT:"Entwurf",SENT:"Wartet auf Antwort",ACCEPTED:"Angenommen",REJECTED:"Abgelehnt",ARCHIVED:"Archiv",CONVERTED:"Auftrag erstellt"} as Row)[v]||v}
function orderStatus(v:string){return ({DRAFT:"Entwurf",PLANNED:"Planung",PREPARATION:"Vorbereitung",IN_PROGRESS:"In Ausführung",READY_FOR_REVIEW:"Chefprüfung",COMPLETED:"Abgeschlossen",INVOICED:"Abgerechnet",CANCELLED:"Storniert"} as Row)[v]||v}
function reportStatus(v:string){return ({DRAFT:"Entwurf",READY_FOR_REVIEW:"Zur Prüfung",APPROVED:"Freigegeben",INVOICED:"Abgerechnet",ARCHIVED:"Archiv"} as Row)[v]||v}
function invoiceStatus(v:string){return ({DRAFT:"Entwurf",REVIEW:"Prüfung",APPROVED:"Freigegeben",SENT:"Rechnung ausstehend",PARTIALLY_PAID:"Teilbezahlt",PAID:"Bezahlt / Archiv",OVERDUE:"Überfällig",CANCELLED:"Storniert"} as Row)[v]||v}
function absenceType(v:string){return ({VACATION:"Urlaub",SICK:"Krank",TRAINING:"Fortbildung",TIME_OFF:"Zeitausgleich",OTHER:"Sonstiges"} as Row)[v]||v}
